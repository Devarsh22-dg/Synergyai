streamlit
pandas
anthropic
python-docx     # For reading/writing .docx files (Word) — including bold/strike formatting and comments
python-pptx     # For reading .pptx files (PowerPoint) — slide text, notes, and embedded images
Pillow          # For resizing/encoding images extracted from documents before sending to Claude vision
openpyxl        # For reading/writing .xlsx files (Excel)
pypdf           # For reading .pdf files, their comment annotations, and embedded images
requests        # For fetching public webpage content (Source URLs)
beautifulsoup4  # For parsing fetched webpage HTML into clean text
streamlit-authenticator  # Login: password hashing + session cookie handling
